# =============================================================================
# Overzeer Environment Variables
# =============================================================================
#
# Port Configuration:
#   - Backend (Elysia): 3000  (SERVER_PORT)
#   - Frontend (Next.js): 3001 (PORT)
#
# URL Flow:
#   Browser → localhost:3001 (Frontend) → localhost:3000 (Backend API)
#
# Docker vs Local:
#   - Docker: Uses this .env file via docker-compose `env_file: - .env`
#   - Local:  Run `bun run dev` from respective backend/frontend directories
#
# =============================================================================

# -----------------------------------------------------------------------------
# Backend (Elysia runtime)
# -----------------------------------------------------------------------------
NODE_ENV=production
SERVER_PORT=3000

# Database - SQLite file path
DATABASE_URL=file:/app/data/local.db

# Better Auth - Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here-min-32-characters
BETTER_AUTH_URL=http://localhost:3000

# CORS - Frontend origin that's allowed to call the backend
CORS_ORIGIN=http://localhost:3001

# -----------------------------------------------------------------------------
# Frontend - Build-time (baked into bundle at `next build`)
# -----------------------------------------------------------------------------
# IMPORTANT: NEXT_PUBLIC_* vars cannot be changed at runtime!
# This is the PUBLIC URL that browsers use to reach the backend.
# For production: https://api.yourdomain.com
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend - Runtime (for SSR inside Docker)
# -----------------------------------------------------------------------------
# This is the INTERNAL Docker network URL for server-side fetch.
# Only needed for Docker deployment - leave empty for local dev.
API_INTERNAL_URL=http://backend:3000

# Frontend port (Next.js listens on this)
PORT=3001